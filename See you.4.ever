<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>通知っぽいページ</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Hiragino Kaku Gothic ProN", sans-serif;
      background: linear-gradient(180deg, #e8ebf0, #f9f9fb);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    #notif {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%) translateY(-30px);
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(20px);
      border-radius: 18px;
      box-shadow: 0 6px 24px rgba(0, 0, 0, 0.2);
      padding: 12px 16px;
      display: flex;
      gap: 12px;
      align-items: center;
      width: calc(100% - 40px);
      max-width: 400px;
      opacity: 0;
      pointer-events: none;
      transition: all 0.4s ease;
      border: 1px solid rgba(0,0,0,0.08);
    }

    #notif.show {
      transform: translateX(-50%) translateY(0);
      opacity: 1;
      pointer-events: auto;
    }

    .icon {
      width: 44px;
      height: 44px;
      background: linear-gradient(135deg, #0a84ff, #4fa3ff);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 16px;
      flex-shrink: 0;
    }

    .content {
      flex: 1;
    }

    .title-row {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
    }

    .title {
      font-weight: 600;
      font-size: 15px;
      color: #111;
    }

    .time {
      font-size: 12px;
      color: #888;
    }

    .message {
      font-size: 14px;
      color: #333;
      margin-top: 2px;
    }

    footer {
      position: fixed;
      bottom: 10px;
      font-size: 12px;
      color: #666;
    }
  </style>
</head>
<body>

<div id="notif" role="alert">
  <div class="icon">🔔</div>
  <div class="content">
    <div class="title-row">
      <div class="title">通知</div>
      <div class="time" id="time">今</div>
    </div>
    <div class="message">人生終了プロトコルを開始します</div>
  </div>
</div>

<footer>※ 本物の通知ではありません。ドッキリ演出です。</footer>

<script>
  // 時刻を表示
  function setCurrentTime() {
    const now = new Date();
    const h = String(now.getHours()).padStart(2, '0');
    const m = String(now.getMinutes()).padStart(2, '0');
    document.getElementById('time').textContent = `${h}:${m}`;
  }

  // 効果音
  function playBeep() {
    try {
      const ctx = new (window.AudioContext || window.webkitAudioContext)();
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'triangle';
      osc.frequency.value = 880;
      gain.gain.value = 0.2;
      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.start();
      osc.stop(ctx.currentTime + 0.25);
    } catch (e) {}
  }

  // 振動
  function vibrate() {
    if (navigator.vibrate) {
      navigator.vibrate([60, 30, 60]);
    }
  }

  function showNotification() {
    setCurrentTime();
    document.getElementById('notif').classList.add('show');
    playBeep();
    vibrate();
    // 5秒後に自動で消す
    setTimeout(() => {
      document.getElementById('notif').classList.remove('show');
    }, 5000);
  }

  window.addEventListener('load', () => {
    setTimeout(showNotification, 700);
  });
</script>

</body>
</html>
